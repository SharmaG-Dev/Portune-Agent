# Copy to .env in the directory where you run the agent.
PORTUNE_SERVER_URL=http://localhost:3000/tunnel
# Set the token issued/accepted by your server. Do not commit real tokens.
PORTUNE_AGENT_TOKEN=
# Generates a display name such as 192.168.1.10-portune-agent.
PORTUNE_AGENT_NAME_PREFIX=portune
