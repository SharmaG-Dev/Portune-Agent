# Optional overrides. The server and name prefix below are built in.
# Automatic token registration requires backend support and is not implemented yet.
# Until then, supply a server-issued token via environment or this optional file.
TUNNEXO_AGENT_TOKEN=
# TUNNEXO_SERVER_URL=https://tunnexo.live/tunnel
# TUNNEXO_AGENT_NAME_PREFIX=tunnexo
